{
  "name": "n4520-player",
  "version": "0.1.0",
  "description": "A HACS-installable Home Assistant dashboard card for a Philips N4520 reel-to-reel styled media player.",
  "main": "dist/philips-n4520-player.js",
  "scripts": {
    "check": "npm run check:app && npm run check:card",
    "check:standalone": "node --input-type=module --check < standalone/app.js",
    "check:app": "npm run check:standalone",
    "check:card": "node --input-type=module --check < dist/philips-n4520-player.js",
    "test": "playwright test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
